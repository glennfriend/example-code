
### Install
```
composer require "winzou/state-machine:^0.4"

or

docker-compose -f docker-compose.yml exec -e COMPOSER_AUTH="{\"github-oauth\": {\"github.com\": \"${GITHUB_PERSONAL_TOKEN}\"}}" php7 composer require "winzou/state-machine:^0.4"
```
